# ─────────────────────────────────────────────────────────────────────────────
#  requirements.txt  —  LangChain Predictive Maintenance Agent
#
#  Install with:  pip install -r requirements.txt
#
#  WHY THESE PINS?
#  ───────────────
#  langchain 1.0+ removed AgentExecutor from langchain.agents entirely.
#  We pin to the 0.3.x series which has:
#    ✅  langchain.agents.AgentExecutor          (stable)
#    ✅  langchain.agents.create_react_agent     (stable)
#    ✅  langchain_core.tools.tool               (stable @tool decorator)
#    ✅  langchain_core.language_models          (BaseChatModel)
#    ✅  langchain_core.prompts.PromptTemplate   (stable)
#    ❌  ConversationBufferWindowMemory          (NOT used — manual deque)
# ─────────────────────────────────────────────────────────────────────────────

# ── LangChain — pinned to 0.3.x series ──────────────────────────────────────
langchain>=0.3.1,<1.0.0
langchain-core>=0.3.0,<1.0.0
langchain-community>=0.3.0,<1.0.0

# ── UI & visualisation ───────────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.20.0
pandas>=2.0.0

# ── HTTP & environment ───────────────────────────────────────────────────────
requests>=2.31.0
python-dotenv>=1.0.0
