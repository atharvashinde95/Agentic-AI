# ─────────────────────────────────────────────────────────────────────────────
#  requirements.txt  —  LangChain Predictive Maintenance Agent (LC 1.0)
#
#  Install:  pip install -r requirements.txt
#
#  LangChain 1.0 API used
#  ──────────────────────
#  from langchain.agents      import create_agent   ✅  (NEW in 1.0)
#  from langchain.tools       import tool           ✅  (stable)
#  from langchain_core.*      import BaseChatModel, messages, etc.
#  langgraph                  — runtime used by create_agent internally
#
#  What was REMOVED in 1.0 (we do NOT use these)
#  ──────────────────────────────────────────────
#  ❌  AgentExecutor            (gone from langchain.agents)
#  ❌  create_react_agent       (gone from langchain.agents → langchain_classic)
#  ❌  ConversationBufferMemory (deprecated since 0.3.1)
# ─────────────────────────────────────────────────────────────────────────────

# ── LangChain 1.0 ────────────────────────────────────────────────────────────
langchain>=1.0.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langgraph>=0.3.0          # runtime used by create_agent under the hood

# ── UI & visualisation ───────────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.20.0
pandas>=2.0.0

# ── HTTP & environment ───────────────────────────────────────────────────────
requests>=2.31.0
python-dotenv>=1.0.0
